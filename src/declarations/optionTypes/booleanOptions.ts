/**
 * @description Options which can be used with boolean field check
 * @param convertToNumber {boolean} Convert bool to number (true = 1, false = 0)
 */

declare type BooleanOptions = {
    // Convert bool to number (true = 1, false = 0)
    convertToNumber?: boolean
}

declare type BooleanOptionsErrors = "";